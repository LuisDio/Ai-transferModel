This project uses Transfer learning model approach as a starting point for modeling a new set of data.

*****************************************************************************************************

The Transfer learning model was already trained on a specific set of data